<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.google.mlkit.vision.DEPENDENCIES" android:value="barcode_ui"/>

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- بازگشت از ورود گوگل (مرورگر سیستم → اپ): بعد از تایید در گوگل، Supabase به همین
                 اسکیم ریدایرکت می‌کند تا کد ورود به‌جای ماندن در مرورگر، به اپ برسد. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ir.novinproduct.samattech" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- برای پیام‌های «درخواست تایید ورود» (login-approval)، سرویس پیش‌فرض خودِ پلاگین
             کپسیتور را با نسخه‌ی خودمان (PushLoginMessagingService) جایگزین می‌کنیم تا بتوانیم
             نوتیفیکیشن را با دو دکمه‌ی واقعی «تایید»/«رد» بسازیم — اندروید فقط اجازه می‌دهد یک
             سرویس برای com.google.firebase.MESSAGING_EVENT ثبت شود، پس اول نسخه‌ی کتابخانه را
             حذف می‌کنیم (tools:node="remove")، بعد نسخه‌ی خودمان را (که برای هر پیام دیگری، دقیقاً
             همان رفتار قبلی پلاگین را با super.onMessageReceived حفظ می‌کند) ثبت می‌کنیم. -->
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />
        <service
            android:name=".PushLoginMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- با لمس دکمه‌ی روی نوتیفیکیشن صدا زده می‌شود — بدون باز کردن هیچ Activity‌ای -->
        <receiver
            android:name=".LoginApprovalActionReceiver"
            android:exported="false" />
    </application>

    <!-- Permissions -->
    <!-- این اپ به دوربین زنده (لایو‌کمرا با واترمارک)، GPS (پیش‌گرم‌سازی مستقیم)، میکروفون
         (یادداشت صوتی حادثه)، و اعلان محلی وابسته است — بدون این مجوزها، همان API‌های وب که در
         مرورگر کار می‌کردند، داخل WebView این اپ کار نخواهند کرد. -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />
</manifest>
